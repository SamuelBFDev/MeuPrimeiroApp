/*
// This code is the initial code generated by the expo-cli
// This is the main file of the app, where the app is initialized and the first screen is created.
// This file is the main entry point of the app.
// This file is the first file that is executed when the app is started.
// The following statements are used to import the necessary modules to create the app.
*/

// This first line imports the StatusBar module from the expo-status-bar package.
import { StatusBar } from 'expo-status-bar';
// The next line imports the StyleSheet, Text, and View modules from the react-native package.
import { StyleSheet, Text, View } from 'react-native';

// The following code is the main function of the app.
// The export keyword is used to export the function as a module.
// The default keyword is used to export the function as the default module.
export default function App() {
  return (
    <View style={styles.container}>
      <Text>There's no such thing as a coincidence.</Text>
      <StatusBar style="auto" />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
